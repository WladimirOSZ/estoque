<h3>Editando o lote <%= @lot.code %></h3>


<% if @lot.errors.any? %>
    <h2>
        Verifique os erros abaixo:
    </h2>
    <ul>
        <% @lot.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
        <% end %>
    </ul>
<% end %>

<%= render 'items_lots_form' %>